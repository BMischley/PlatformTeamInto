function ClassDiv({ course, show, none }) {
   // Use the show prop to conditionally render the div and display the course
   // HINT: If you want to include js in the retun statement include it in {}
   
    return (
      <>
        
      </>
    );
  }
  
  export default ClassDiv;